# 论文主要研究怎么从结构化表中提取结构化的层级概念
论文中叫conceptual schema interfence，并提出了两种主要的方法，一个叫GeSI，基于llm，通过抽样的表的数据让llm来构造表的概念层次，一个是EmSI，这个是先将表、列做embedding后，通过聚类的方法生成表的概念层次

它主要提出了怎么提取结构化概念层次的方法，但是提取出来后，这个概念层次应该怎么被使用，怎么检索，这个关键的问题，论文中没有提到
